# VinCert dependencies
# Target: Windows 10/11 x64, Python 3.11 (recommended) or 3.10
# Also runs on macOS for UI / digital-PDF development (OCR packages are Windows-marked).
#
# Setup (Windows):
#   py -3.11 -m venv .venv
#   .venv\Scripts\activate
#   python -m pip install --upgrade pip
#   python -m pip install paddlepaddle==2.6.2 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
#   pip install -r requirements.txt
#   python gui.py
#
# Notes:
# - Official Python installer: enable "tcl/tk and IDLE" (CustomTkinter needs tkinter).
# - Prefer 64-bit Python; PyMuPDF / Pillow / Paddle ship win_amd64 wheels.
# - PaddleOCR is used only when a PDF cover has no embedded text (scanned certs).
# - First OCR run downloads detection/recognition models (needs network once).
# - macOS: core packages install; Windows-only OCR markers are skipped.

# --- UI ---
customtkinter==6.0.0

# --- PDF / images ---
pymupdf==1.28.0
Pillow==12.3.0
openpyxl>=3.1.5,<4.0

# --- OCR (Windows CPU; same stack family as 证书扫描/ji_liang) ---
# Install paddlepaddle from the official CPU index BEFORE this file if PyPI
# does not resolve the wheel (see Setup above).
numpy>=1.24.0,<2.0.0; sys_platform == "win32"
opencv-python>=4.6.0.66,<4.10; sys_platform == "win32"
paddlepaddle==2.6.2; sys_platform == "win32"
paddleocr==2.7.3; sys_platform == "win32"
